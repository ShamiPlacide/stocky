// Empty = relative to current host (works for local Django + Codespaces).
// When deploying frontend to Vercel, set this to your Render backend URL.
window.API_BASE = "";
window.LOW_STOCK_THRESHOLD = 5;
